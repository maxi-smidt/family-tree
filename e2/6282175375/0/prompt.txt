[P2] Other semantic 401s still invalidate the session. The classifier exempts only public_password_required, but the next step in that same flow returns 401 invalid_public_password when the entered public password is wrong. For a signed-in visitor, one typo therefore still opens the non-dismissible re-login dialog over the public-password form. The authenticated import flow also uses a semantic 401 Password required. Exempt all known semantic responses—or preferably let specific requests opt out of global credential handling—and add a wrong-public-password regression test. api.ts:139