[P1] Existing version-2 backups become unrestorable. Adding DocumentUpload to BACKUP_MODELS changes the exact table-name set required by validate_bundle(), while BACKUP_VERSION remains 2. Every backup created before this PR lacks document_uploads in both tables and manifest.table_row_counts, so it will now fail with “Backup does not contain every required table.” Normalize legacy v2 bundles by inserting an empty document_uploads table/count, or introduce explicit versioned migration logic and retain v2 restore support. Add a legacy-bundle regression test. backup_service.py:111