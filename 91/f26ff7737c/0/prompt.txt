[P2] The timeout path is tested but no runtime timeout exists. checkAuthSession() calls api.get("/auth/me") without a timeout, and init() first awaits the similarly unbounded /auth/config request. Since the API client only creates an AbortController when timeoutMs is supplied—and api.get cannot supply one—a stalled connection can leave the app on the loading spinner forever without reaching unreachable. The DOMException test merely injects a rejection. Add a bounded timeout to both startup requests. useAuthStore.ts:146