name: Checks

# Reusable: build/test/lint everything and report whether it passed. Called
# by check-build.yml on every push/PR — including the release workflow's
# release PRs, so a release exercises this exact suite before it can merge.

on:
  workflow_call:
    inputs:
      run-all:
        description: >-
          Run every area's checks regardless of changed paths. Release runs
          must always validate the full app, not just what a diff touched.
        type: boolean
        default: false
        required: false
    outputs:
      frontend_result:
        description: "Result of the frontend job (success/skipped/failure/...)."
        value: ${{ jobs.result.outputs.frontend_result }}
      passed:
        description: "true if every required check passed (or was legitimately skipped)."
        value: ${{ jobs.result.outputs.passed }}

permissions:
  contents: read
  pull-requests: read

jobs:
  # Detect which areas a PR touched so the irrelevant heavy jobs can be
  # skipped. On push/tag/run-all we always run everything (full validation).
  changes:
    name: "Detect changed areas"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      backend: ${{ steps.filter.outputs.backend }}
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v7

      - name: "Filter changed paths"
        uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            frontend:
              - 'frontend/**'
              - '.github/workflows/checks.yml'
              - '.github/workflows/check-build.yml'
            backend:
              - 'backend/**'
              - '.github/workflows/checks.yml'
              - '.github/workflows/check-build.yml'

  frontend:
    name: "Frontend (build + test + i18n)"
    needs: changes
    if: ${{ inputs.run-all || github.event_name != 'pull_request' || needs.changes.outputs.frontend == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v7

      - name: "Set up Node.js"
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: "Install dependencies"
        run: npm ci

      - name: "Type-check & build"
        run: npm run build

      - name: "Bundle size budget"
        run: npm run check-bundle-size

      - name: "Unit tests"
        run: npx vitest run

      - name: "Translation parity"
        run: npm run check-i18n

      - name: "No explicit any"
        run: npm run check-no-any

  backend:
    name: "Backend (deps + checks)"
    needs: changes
    if: ${{ inputs.run-all || github.event_name != 'pull_request' || needs.changes.outputs.backend == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v7

      - name: "Set up uv"
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: "3.12"

      - name: "Install dependencies"
        run: uv sync --frozen

      - name: "Lint (ruff)"
        run: uv run ruff check

      - name: "Compile & import checks"
        run: |
          uv run python -m compileall -q app alembic
          uv run python -c "import app.main; print('app imports OK')"

      - name: "Unit tests"
        # -n auto fans the suite across the runner's cores (pytest-xdist).
        # Tests are isolation-safe: each gets its own SQLite DB via tmp_path.
        run: uv run pytest -n auto

  compose-config:
    name: "Compose backend env propagation"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v7

      # Renders docker-compose.yml / docker-compose.prod.yml with sentinel
      # values for every documented backend setting and asserts each one
      # reaches the backend service's *rendered* environment — catches
      # settings that are readable via .env but missing from a compose
      # file's `environment:` list (and so never reach the container).
      - name: "Render compose configs with sentinel values"
        run: |
          set -euo pipefail
          cat > /tmp/sentinel.env <<'EOF'
          SECRET_KEY=sentinel-secret-key-0123456789abcdef
          DATABASE_URL=sentinel-database-url
          POSTGRES_HOST=sentinel-postgres-host
          POSTGRES_PORT=15432
          POSTGRES_USER=sentinel-postgres-user
          POSTGRES_PASSWORD=sentinel-postgres-password
          POSTGRES_DB=sentinel-postgres-db
          DB_POOL_SIZE=42
          DB_MAX_OVERFLOW=43
          DB_POOL_RECYCLE=4400
          FRONTEND_URL=http://sentinel-frontend
          CORS_ORIGINS=http://sentinel-cors
          ALLOW_SELF_REGISTRATION=true
          FIRST_ADMIN_USERNAME=sentinel-admin-user
          FIRST_ADMIN_PASSWORD=sentinel-admin-password
          FIRST_ADMIN_EMAIL=sentinel@example.com
          AUTHENTIK_CLIENT_ID=sentinel-client-id
          AUTHENTIK_CLIENT_SECRET=sentinel-client-secret
          AUTHENTIK_DISCOVERY_URL=http://sentinel-discovery-url
          AUTHENTIK_SCOPES=sentinel-scopes
          AUTHENTIK_AUTO_CREATE_USERS=false
          AUTHENTIK_ADMIN_GROUP=sentinel-admin-group
          REDIS_URL=redis://sentinel-redis:6379/0
          WORKERS=7
          EOF

          for f in docker-compose.yml docker-compose.prod.yml; do
            echo "== $f =="
            docker compose --env-file /tmp/sentinel.env -f "$f" config --format json \
              | jq '.services.backend.environment' > /tmp/backend-env.json
            while IFS='=' read -r key expected; do
              [ -z "$key" ] && continue
              actual=$(jq -r --arg k "$key" '.[$k]' /tmp/backend-env.json)
              if [ "$actual" != "$expected" ]; then
                echo "::error::$f: backend service environment has $key='$actual', expected the sentinel '$expected' — is $key missing from environment: in $f?"
                exit 1
              fi
            done < /tmp/sentinel.env
          done
          echo "Every documented backend setting reaches the backend service environment in both compose files."

  version-policy:
    name: "Version metadata policy"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v7

      - name: "Check package metadata"
        run: |
          PACKAGE_VERSION=$(jq -r .version frontend/package.json)
          LOCK_VERSION=$(jq -r .version frontend/package-lock.json)
          LOCK_ROOT_VERSION=$(jq -r '.packages[""].version' frontend/package-lock.json)
          PYPROJECT_VERSION=$(sed -n 's/^version = "\(.*\)"$/\1/p' backend/pyproject.toml | head -1)
          UV_LOCK_VERSION=$(grep -A1 '^name = "family-tree-backend"$' backend/uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')

          echo "frontend/package.json:      $PACKAGE_VERSION"
          echo "frontend/package-lock.json: $LOCK_VERSION"
          echo "lock root package:          $LOCK_ROOT_VERSION"
          echo "backend/pyproject.toml:     $PYPROJECT_VERSION"
          echo "backend/uv.lock:            $UV_LOCK_VERSION"

          if [ "$PACKAGE_VERSION" != "$LOCK_VERSION" ] || [ "$PACKAGE_VERSION" != "$LOCK_ROOT_VERSION" ] \
            || [ "$PACKAGE_VERSION" != "$PYPROJECT_VERSION" ] || [ "$PACKAGE_VERSION" != "$UV_LOCK_VERSION" ]; then
            echo "Version metadata is out of sync across frontend/backend."
            echo "Run 'npm run bump:patch|minor|major' from frontend/ — it updates all four files."
            exit 1
          fi

          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            TAG_VERSION="${GITHUB_REF_NAME#v}"
            echo "Release tag:                $TAG_VERSION"
            if [ "$TAG_VERSION" != "$PACKAGE_VERSION" ] || [ "$TAG_VERSION" != "$LOCK_VERSION" ] \
              || [ "$TAG_VERSION" != "$LOCK_ROOT_VERSION" ] || [ "$TAG_VERSION" != "$PYPROJECT_VERSION" ] \
              || [ "$TAG_VERSION" != "$UV_LOCK_VERSION" ]; then
              echo "Release tag v$TAG_VERSION does not match all release metadata."
              exit 1
            fi
          else
            echo "Ordinary PRs and main pushes do not need a version bump."
          fi

  # Aggregate gate exposed to callers as outputs.passed / outputs.frontend_result.
  result:
    name: "Aggregate result"
    needs: [changes, frontend, backend, compose-config, version-policy]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      frontend_result: ${{ needs.frontend.result }}
      passed: ${{ steps.gate.outputs.passed }}
    steps:
      - name: "Require all checks to pass"
        id: gate
        run: |
          echo "changes=${{ needs.changes.result }} frontend=${{ needs.frontend.result }} backend=${{ needs.backend.result }} compose-config=${{ needs.compose-config.result }} version-policy=${{ needs.version-policy.result }}"
          # Change-detection must succeed, else we'd skip everything and pass blindly.
          if [ "${{ needs.changes.result }}" != "success" ]; then
            echo "Change detection failed."
            exit 1
          fi
          # frontend/backend may be legitimately "skipped" when their area was untouched.
          for res in "${{ needs.frontend.result }}" "${{ needs.backend.result }}"; do
            if [ "$res" != "success" ] && [ "$res" != "skipped" ]; then
              echo "A build/test job failed ($res)."
              exit 1
            fi
          done
          # The always-on gate jobs must pass outright.
          if [ "${{ needs.compose-config.result }}" != "success" ] || [ "${{ needs.version-policy.result }}" != "success" ]; then
            echo "A required gate job failed."
            exit 1
          fi
          echo "All checks passed (skipped = no changes in that area)."
          echo "passed=true" >> "$GITHUB_OUTPUT"
