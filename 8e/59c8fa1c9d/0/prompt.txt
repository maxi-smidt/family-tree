[P1] A stalled Redis connection still makes optional Redis fail readiness. ping_redis() has no application timeout, and the Redis client is created without socket connect/read timeouts. If Redis traffic is silently dropped rather than immediately refused, /health/ready remains pending until the Compose probe’s five-second timeout, so Docker still marks the backend unhealthy before the code can return the new degraded 200. Startup can similarly stall while awaiting start_redis_listener(). Bound Redis connection operations with a short timeout and treat timeout as unavailable when Redis is optional; add a test where the ping never resolves. main.py:169