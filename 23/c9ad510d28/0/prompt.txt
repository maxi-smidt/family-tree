[P2] Failed callbacks are retained and can run after a later, unrelated commit. UnitOfWork.__exit__ never clears _callbacks. If the same instance is reused after a failed block, callbacks belonging to the rolled-back mutation execute after the next successful commit. Successful reuse also republishes old callbacks. Clear the queue on rollback and drain it before executing callbacks; add a reuse-after-failure regression test.